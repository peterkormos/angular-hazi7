import { DisplayTaskPipe } from './display-task.pipe';

describe('DisplayTaskPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayTaskPipe();
    expect(pipe).toBeTruthy();
  });
});
